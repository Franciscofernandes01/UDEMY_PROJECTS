const verificarValor = function(valor) {
    if (valor >= 7) {
        console.log("Aprovado com " + valor);
    } else {
        console.log("Reprovado com " + valor);
    }
}

verificarValor(8);
verificarValor(6);